# Phantom Problems Bot

For setup, create `.env` file with `TOKEN` containing the bot token and database according to https://www.prisma.io/docs/getting-started

Current features:
- Create weekly attendance poll